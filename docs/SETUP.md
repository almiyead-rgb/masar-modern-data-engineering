<table dir="ltr" width="100%"><tr><td width="50%" valign="top" dir="ltr" lang="en" align="left"><h1>Day 1 setup</h1><p><a href="../day01/README.md">Day 1 guide</a> · <a href="../STATUS.md">Execution record</a></p></td><td width="50%" valign="top" dir="rtl" lang="ar" align="right"><h1>إعداد اليوم الأول</h1><p><a href="../day01/README.md">دليل اليوم الأول</a> · <a href="../STATUS.md">سجل التنفيذ</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" valign="top" dir="ltr" lang="en" align="left"><h2>Scope and tested environment</h2><p>Day 1 uses CPU, Python, Java, Spark and Delta only. Do not install Docker, Kafka or dbt for these two labs. No paid API, cloud subscription or access token is needed. Internet is required for initial packages and Delta JAR downloads.</p><p>The complete Day 1 notebook has passed two independent runs on GitHub with Python 3.11, Java 17, Spark 3.5.8 and Delta 3.3.2. This is not a separate test of Colab's hosted environment or every local operating system. <a href="../day01/verification.json">Read the actual verification record</a>.</p></td><td width="50%" valign="top" dir="rtl" lang="ar" align="right"><h2>النطاق والبيئة المختبرة</h2><p>يستخدم اليوم الأول CPU وPython وJava وSpark وDelta فقط. لا تثبت Docker أو Kafka أو dbt لهذين اللابين. لا تحتاج إلى API مدفوع أو اشتراك سحابي أو رمز وصول. يلزم الإنترنت للتنزيل الأول للحزم وملفات Delta JAR.</p><p>اجتاز دفتر اليوم الأول كاملًا تشغيلين مستقلين على GitHub باستخدام Python 3.11 وJava 17 وSpark 3.5.8 وDelta 3.3.2. لا يُعد ذلك اختبارًا مستقلًا لخدمة Colab المستضافة أو لكل أنظمة التشغيل المحلية. <a href="../day01/verification.json">اقرأ سجل التحقق الفعلي</a>.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" valign="top" dir="ltr" lang="en" align="left"><h2>Colab CPU route</h2><p>Open <a href="https://colab.research.google.com/github/almiyead-rgb/masar-modern-data-engineering/blob/main/DAY01_STUDENT.ipynb">the notebook in Colab</a>, save a copy to your Drive, use a CPU runtime and run from the first cell. The setup cell clones this public repository, installs the specified PySpark/Delta packages and selects Java 17. A Google account is needed to use Colab, but a paid subscription is not part of the lab requirements.</p><p>Save your notebook with outputs and download the generated handoff ZIP before ending the session. Colab's installed software and available resources may differ from the tested GitHub environment; a setup error must be resolved rather than skipped.</p></td><td width="50%" valign="top" dir="rtl" lang="ar" align="right"><h2>مسار Colab بمعالج CPU</h2><p>افتح <a href="https://colab.research.google.com/github/almiyead-rgb/masar-modern-data-engineering/blob/main/DAY01_STUDENT.ipynb">الدفتر في Colab</a> واحفظ نسخة في Drive واختر بيئة CPU وشغّل من أول خلية. تستنسخ خلية الإعداد هذا المستودع العام وتثبت إصدارات PySpark وDelta المحددة وتختار Java 17. يلزم حساب Google لاستخدام Colab، لكن الاشتراك المدفوع ليس من متطلبات اللاب.</p><p>احفظ الدفتر بمخرجاته ونزّل ملف الانتقال قبل إنهاء الجلسة. قد تختلف برامج Colab المثبتة وموارده المتاحة عن بيئة GitHub المختبرة؛ يجب معالجة خطأ الإعداد لا تجاوزه.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" valign="top" dir="ltr" lang="en" align="left"><h2>Local Jupyter route</h2><p>Use Python 3.11 and Java 17. Set <code>JAVA_HOME</code> to the Java 17 installation and confirm <code>java -version</code> before starting Jupyter. Clone the repository and create a dedicated Python environment. The following commands use a Linux/macOS shell; on Windows Command Prompt use <code>py -3.11 -m venv .venv</code> and <code>.venv\Scripts\activate</code> instead.</p></td><td width="50%" valign="top" dir="rtl" lang="ar" align="right"><h2>مسار Jupyter المحلي</h2><p>استخدم Python 3.11 وJava 17. اضبط <code>JAVA_HOME</code> على تثبيت Java 17 وتحقق بأمر <code>java -version</code> قبل فتح Jupyter. استنسخ المستودع وأنشئ بيئة Python مخصصة. الأوامر التالية لطرفية Linux أو macOS؛ وفي موجه أوامر Windows استخدم بدلًا منها <code>py -3.11 -m venv .venv</code> و<code>.venv\Scripts\activate</code>.</p></td></tr></table>

```bash
git clone https://github.com/almiyead-rgb/masar-modern-data-engineering.git
cd masar-modern-data-engineering
python3.11 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements-day01.txt
python -m pip check
python -m jupyterlab
```

<table dir="ltr" width="100%"><tr><td width="50%" valign="top" dir="ltr" lang="en" align="left"><h2>Run and retain your work</h2><p>Open <code>DAY01_STUDENT.ipynb</code> using the kernel from that environment. Run the entire notebook in order; shared code is under <code>src/masar/</code> and sources are under <code>data/masar-small-v1/</code>. Reruns create separate workspaces and do not delete earlier work. Do not replace Delta tables with ordinary JSON files.</p><p>Save the executed notebook, your lab notes and <code>outputs/day01_handoff.zip</code>. See <a href="../day01/COMPLETION.md">completion</a> and <a href="TROUBLESHOOTING.md">troubleshooting</a>. Source installation alone is not lab completion; the notebook's assertions and reports must succeed.</p></td><td width="50%" valign="top" dir="rtl" lang="ar" align="right"><h2>شغّل واحتفظ بعملك</h2><p>افتح <code>DAY01_STUDENT.ipynb</code> بنواة البيئة نفسها. شغّل الدفتر كاملًا بالترتيب؛ الكود المشترك تحت <code>src/masar/</code> والمصادر تحت <code>data/masar-small-v1/</code>. تنشئ الإعادة مساحات عمل مستقلة دون حذف السابق. لا تستبدل جداول Delta بملفات JSON عادية.</p><p>احفظ الدفتر المنفذ وملاحظاتك وملف <code>outputs/day01_handoff.zip</code>. راجع <a href="../day01/COMPLETION.md">الاكتمال</a> و<a href="TROUBLESHOOTING.md">معالجة الأخطاء</a>. تثبيت المصادر وحده لا يعني إكمال اللاب؛ يجب نجاح فحوص الدفتر وتقاريره.</p></td></tr></table>
